import 'package:flutter/cupertino.dart';import 'package:flutter/material.dart';class QandA extends StatefulWidget {  const QandA({Key? key}) : super(key: key);  @override  State<QandA> createState() => _QandAState();}class _QandAState extends State<QandA> {  @override  Widget build(BuildContext context) {    return Scaffold(      backgroundColor: Colors.white,      //APPBar 상단 Appbar 지정 및 설정      appBar: AppBar(        title: const Text('Q&A'),        centerTitle: true,        elevation: 0.0,        backgroundColor: Colors.grey,        actions: [          IconButton(              onPressed: () => {print("hi22")},              icon: const Icon(Icons.shopping_cart)),          IconButton(              onPressed: () => {print("hi33")}, icon: const Icon(Icons.search)),        ],      ),      body: Container(      ),    );  }}